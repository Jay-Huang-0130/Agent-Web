#!/usr/bin/env bash
set -Eeuo pipefail

if [[ -x /usr/local/bin/agent-webctl ]]; then
    exec /usr/local/bin/agent-webctl "$@"
fi

echo "Agent Web is not installed. Run ./install.sh first." >&2
exit 69
